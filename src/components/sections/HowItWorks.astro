---
import { siteConfig } from "../../config/site";

const { howItWorks } = siteConfig;
---

<section class="bg-muted px-6 py-16 sm:px-8 lg:px-12" aria-labelledby="how-it-works-heading">
  <div class="mx-auto max-w-6xl">
    <div class="max-w-2xl">
      <p class="font-display text-sm font-semibold uppercase tracking-normal text-primary">
        Process
      </p>
      <h2 id="how-it-works-heading" class="mt-4 font-display text-3xl font-bold leading-tight tracking-normal text-foreground sm:text-4xl">
        {howItWorks.heading}
      </h2>
    </div>

    <ol class="relative mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-4 lg:gap-4">
      <span class="absolute left-12 right-12 top-8 hidden h-px bg-border lg:block" aria-hidden="true"></span>
      {
        howItWorks.steps.map((step, index) => (
          <li class="relative">
            <article class="h-full rounded-lg border border-border bg-card p-6 shadow-soft">
              <div class="flex h-16 w-16 items-center justify-center rounded-lg border border-primary-subtle bg-primary-soft font-display text-lg font-bold text-primary">
                {String(index + 1).padStart(2, "0")}
              </div>
              <h3 class="mt-6 font-display text-xl font-semibold tracking-normal text-foreground">
                {step.heading}
              </h3>
              <p class="mt-3 text-base leading-7 text-muted-foreground">
                {step.body}
              </p>
            </article>
          </li>
        ))
      }
    </ol>
  </div>
</section>
